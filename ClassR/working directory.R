# - this is a comment,
#put (#) before the line to comment it out so that R doesn't evalate it as code

#Check working directory----
getwd()

#Change/Set working directory----
# do ||    setwd("Path")    || example:
setwd(dir =  "J:\\Personalprojects\\Teaching\\Classer\\packrat") #Use double back slash

setwd(dir = "J:/Personalprojects/Teaching/Classer") #Use single forward slash


