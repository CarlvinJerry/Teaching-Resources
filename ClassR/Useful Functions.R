#Useful Functions-----
length(object)#Checks the number of elemens/components in an object
dim(object) #Checks the didmension of a df or matrix

class(object) #checks for class or type of an object
names(object) #checks for col names
str(df2) #Checks the structure of an object

df3 <- df1
df3
ls() #Lists current objects
rm(df3) #Removes listed objects

newdf <- edit(df1) #Edit a df
fix(newdf) #Edit in place
